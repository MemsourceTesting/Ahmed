phrase:
  project_id: "d2d1deeecdfa41f343cf012bb225b24d"
  push:
    sources:
    - file: ./source/<tag>.en1.yml
      params:
        file_format: simple_json
        locale_id: English #Can be the name (e.g. en-US) or public id (via Language settings -> API) of the locale. Preferred is the public id.
        update_translations: true #Indicates whether existing translations should be updated with the file content.
        #tags: github_1
        autotranslate: true 
        encoding: UTF-8
        - file: ./sources/<tag>.en2.yml
      params:
        file_format: simple_json
        locale_id: English #Can be the name (e.g. en-US) or public id (via Language settings -> API) of the locale. Preferred is the public id.
        update_translations: true #Indicates whether existing translations should be updated with the file content.
        #tags: github_2
        autotranslate: true 
        encoding: UTF-8
  pull:
    targets:
    - file: ./target/locales/github_1.<locale_name>.json
      params:
        file_format: simple_json
        locale_id: German
        include_empty_translations: true #Indicates whether keys without translations should be included in the output as well.
        encoding: UTF-8
        tags: github_1
    - file: "./target/locales/github_1.<locale_name>.json"
      params:
        file_format: simple_json
        locale_id: 577d28969ced69d376d0dc9fde3bf938 
        include_empty_translations: true 
        encoding: UTF-8
        tags: github_2
